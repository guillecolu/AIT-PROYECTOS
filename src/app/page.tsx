import AutoLoginForm from '@/components/auth/auto-login-form';

export default function LoginPage() {
    return (
        <main className="flex items-center justify-center min-h-screen bg-muted/40 p-6">
            <AutoLoginForm />
        </main>
    );
}
